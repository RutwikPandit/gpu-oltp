# ---- build artifacts (all regenerable from source) ----
*.so
*.o
*.bc
*.out
oltp_bench
cpu_sweep
membench
doorbell
doorbell_native
rgi_sweep
rgi_persist
rgi_persist2
rgi_prof
rgi_prof2
rgi_profile_one

# ---- Nsight profiler binaries (extracted CSV/TXT summaries are kept) ----
*.ncu-rep
*.qdstrm

# ---- large regenerable slide export (the .md + .pdf are kept) ----
*.pptx

# ---- secrets / private keys / local-only (defense in depth) ----
VULTR_API.txt
*.pem
id_*
*_ed25519
*_rsa
internal/

# ---- superseded/historical docs: kept locally, not pushed to teammates ----
# (remove this line if you want the project history public)
archive/

# ---- editor / OS ----
/debug.log
.DS_Store
*.swp
